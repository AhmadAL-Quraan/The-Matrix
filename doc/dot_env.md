## Environment variables in python 

* Environment variables used to hide important/ secret information, like api keys, secrets ,... 
* To load it in current code use `load_dotenv(<path_file>)` . 
* To get a specific variable from it, use `os.getenv(variable)`
* Check if a file exists or not `os.path.isfile(path)`
