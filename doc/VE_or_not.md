## Virtual environment

* An isolated folder structure on your computer that contains its own Python interpreter and independent set of installed packages.

* Activate it in python `python3 -m venv <path>`.
* `source <path>/bin/activate`
